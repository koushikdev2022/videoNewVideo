const express = require("express");
const videoAttributeRoute = express.Router();


const videoAttributeController = require("../../controller/api/user/video/videoAttribute.controller")

module.exports = videoAttributeRoute;